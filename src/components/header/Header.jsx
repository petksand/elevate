import React from 'react';
import { Layout } from 'antd';

const { Header: AntHeader } = Layout;

export const Header = () => {
    return (
        <AntHeader>

        </AntHeader>
    );
};

export default Header;
